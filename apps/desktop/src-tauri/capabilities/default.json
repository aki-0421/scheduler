{
  "identifier": "default",
  "description": "Default permissions for the Codex Scheduler main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "notification:default"
  ]
}

{
  "identifier": "default",
  "description": "Default permissions for the Clockhand main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "notification:default"
  ]
}
